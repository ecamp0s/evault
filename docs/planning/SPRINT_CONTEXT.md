SPRINT CONTEXT — eVault
Actualizado: 16 de septiembre de 2026
Estado: Iteración 18 cerrada el 16 de septiembre de 2026 con su objetivo cumplido: la vault se abre desde la barra del navegador, con la extensión de Chrome y Windows Hello, sobre la instancia de kastor. La 19 está sin planificar.

Nota de formato: este documento está escrito en prosa plana sin Markdown, siguiendo la convención del proyecto para instrucciones dirigidas a Claude Code.

Este archivo es el puente entre sesiones y se lee entero al empezar. Por eso es corto, y hay que mantenerlo corto: creció hasta las cuatrocientas cincuenta líneas durante la Iteración 1 y dejó de cumplir su función, porque lo único que se leía eran las últimas veinte. Volvió a crecer hasta las 60.000 letras y lo recortó el 664, que además puso un techo en check-docs.py para que la próxima vez lo diga un comando y no una lectura. Lo que no cabe aquí vive en otro sitio y se enlaza.

Qué NO se escribe aquí. Ni qué issues están cerrados ni cuál es el siguiente: eso se lee en docs/planning/STATUS.md, que se genera desde GitHub. Ni el entorno local, que está en docs/development/SETUP.md. Ni el historial de lo ya hecho, que se archiva por iteración en docs/planning/archive/. Una copia se desincroniza siempre, porque nada obliga a actualizarla.


QUÉ ES eVault

eVault es un gestor de contraseñas y secretos personales con modelo zero-knowledge. El servidor nunca puede leer los datos del usuario: toda la criptografía ocurre en el cliente antes de que los datos salgan del dispositivo, y la base de datos solo almacena blobs cifrados opacos.

NO se comercializa, y eso está decidido en ADR-009. Los dos propósitos reales son que el desarrollador lo use para sus propias contraseñas en una instancia self-hosted, y que el repositorio sea público y sirva como muestra de trabajo en procesos de selección. Quien lo lea estará evaluando criterio técnico: código, decisiones de seguridad, arquitectura y documentación. De ahí que el self-hosting sea el único modo de despliegue y que ADR-005 gane importancia en lugar de perderla.

Quedan fuera del alcance, y conviene no reabrirlos por inercia: vaults compartidas, organizaciones, plan Team y el panel Filament de administración de plataforma. El multi-tenancy ya construido NO se retira, porque el aislamiento cross-tenant con sus tests es precisamente lo que hay que poder enseñar.

Los clientes previstos siguen siendo una SPA web, una app nativa iOS/Android y una extensión de navegador para Chrome. La web está construida; la extensión se está construyendo en la Iteración 18; la app nativa no se ha empezado.


DÓNDE ENCONTRAR CADA COSA

Estado del backlog, prioridades y dependencias: docs/planning/STATUS.md, generado desde GitHub.
Entorno local, stack, versiones y arranque: docs/development/SETUP.md.
Por qué el proyecto está construido así: los veintitrés ADR en docs/architecture/decisions.
Historial de iteraciones cerradas y sus lecciones: docs/planning/archive.
Modelo de datos y contrato del blob: docs/architecture/FOUNDATION.md, lectura obligatoria antes de tocar la API o de añadir una columna a vault_items.
Qué llave abre qué y qué se pierde con cada una: docs/architecture/KEYS.md, que es de consulta y responde sin abrir ningún ADR.
Comandos, URLs, workflow git e idioma del código: CLAUDE.md en la raíz.
Reglas de la propia documentación: docs/GUIDE.md.


DECISIONES DE ARQUITECTURA CERRADAS

Los veintitrés ADR de docs/architecture/decisions son la fuente de verdad, y son inmutables: si una decisión cambia, se escribe uno nuevo que la supersede. Lo que sigue es el índice para saber cuál abrir, no un sustituto de abrirlo.

Los seis primeros están numerados por profundidad arquitectónica y no por fecha. ADR-001 zero-knowledge. ADR-002 React para la vault y Filament solo para administración, porque el server-side rendering rompería la garantía. ADR-003 monorepo. ADR-004 multi-tenancy sin Spatie teams, con el contexto de tenant explícito en cada llamada porque la API es stateless. ADR-005 arquitectura self-hosteable. ADR-006 TypeScript 6, con un bloqueador verificable detrás.

A partir del 007 la numeración es cronológica. ADR-007 el token de sesión vive solo en memoria, así que recargar no es una expulsión sino el bloqueo de la vault. ADR-008 arquitectura de claves. ADR-009 el proyecto deja de ser un SaaS. ADR-010 clave de recuperación. ADR-011 formato de export e import. ADR-012 estrategia de despliegue. ADR-013 emplazamiento y operación de la instancia personal, que es además donde queda corregida la imprecisión de ADR-012 sección 2.3 al meter Tailscale, Cloudflare y una VPN propia en el mismo saco. ADR-014 cambio de correo electrónico, y de ahí lo único que hay que tener presente sin abrirlo: cambiar el correo SÍ invalida la clave de recuperación, al contrario que rotar la contraseña maestra, porque el correo es el salt del HKDF que deriva sus claves. ADR-015 acceso a la vault desde fuera de la red local, que elige Tailscale y explica por qué el criterio no es la comodidad sino quién puede servir el JavaScript. ADR-016 un solo origen para la SPA y la API, que mueve la API a /api del mismo host y retira CORS, y de ahí lo que hay que tener presente: dos lineamientos de ADR-012 sección 4 dejaron de regir y siguen escritos ahí, porque los ADR son inmutables. ADR-017 los códigos TOTP dentro de la vault, y de ahí lo que hay que tener presente sin abrirlo: SÍ se guardan semillas, dentro del item y del blob cifrado, asumiendo que quien abra la vault tiene también los segundos factores; no sube ninguna de las dos versiones, ni la del esquema criptográfico ni la del fichero de export; y la semilla NO sale nunca en el export en claro, que además dice a cuántas entradas afecta. ADR-018 qué se conserva después de decir que ya no lo quieres —historial de contraseñas, papelera y caducidad de sesión—, y de ahí lo que hay que tener presente sin abrirlo: está APROBADO Y PARCIALMENTE EN VIGOR desde la Iteración 17. Su HISTORIAL DE CONTRASEÑAS rige, porque ADR-022 lo necesita para no perder nada al reconciliar; su PAPELERA y su CADUCIDAD DE TOKEN a 12 horas siguen diferidas, y no por olvido: no las necesita nada de la 17 y arrastrarlas por venir en el mismo documento sería implementarlas juntas cuando lo que se agrupó fue decidirlas. SU CAMPO DEL BLOB SE LLAMA history Y NO historial, que es como el documento lo nombra veinticuatro veces. Y su regla de que el import NUNCA crea historial queda revisada por ADR-022 en un alcance estrecho: sigue valiendo para un import de una sola fuente, y lo que se admite es que una RECONCILIACIÓN escriba en él, porque ahí las dos contraseñas existen de verdad y lo que se desconoce es cuál es la actual. ADR-019 la vault sin red, que es el que decide la Iteración 14. ADR-020 los tipos de entrada de la vault, que es el que decide la Iteración 15, y de ahí lo que hay que tener presente sin abrirlo: el tipo vive DENTRO del blob y su ausencia significa login, así que las entradas anteriores no se migran; una columna type en vault_items está descartada porque diría cuántas tarjetas tiene cada usuario sin descifrar nada; el código de seguridad se llama csc y no cvv, porque CVV2 es de Visa y los de American Express son CUATRO dígitos y no tres; la tarjeta se acota por tamaño y NO se valida por forma, porque una Amex tiene además 15 dígitos y no 16; y los documentos adjuntos quedan FUERA, porque GET /items devuelve todos los items sin paginar y un adjunto se descargaría entero en cada carga. ADR-021 desbloquear la vault con un passkey, que es el que decide la Iteración 16, y de ahí lo que hay que tener presente sin abrirlo: el passkey envuelve la MISMA clave de vault y no guarda ni sustituye la contraseña maestra, que sigue siendo el camino principal; el material de partida es la extensión PRF de WebAuthn, con userVerification required SIN modo degradado, porque es lo único que hace que poseer el PRF pruebe que la persona estuvo delante; el salt del HKDF es el correo normalizado y NO por simetría con ADR-010, sino porque el hash de autenticación se deriva ANTES de tener token y cualquier otro salt habría que pedirlo —lo que obliga a un oráculo de enumeración de cuentas o a guardarlo en el dispositivo, que mata el desbloqueo desde un segundo aparato—; y de ahí que cambiar el correo SÍ revoque los passkeys mientras que rotar la contraseña maestra NO, igual que con la clave de recuperación. El servidor NO verifica WebAuthn y recibe un hash donde un revisor esperaría una firma: el argumento es que el PRF solo se produce tras la verificación de usuario, y eso descansa en que el token solo trae ciphertext, premisa que queda escrita como trigger. Son varios passkeys y no uno, con tabla propia, porque una credencial de iCloud Keychain no existe en Chrome ni en Firefox sobre Windows. Y no sube ninguna de las dos versiones ni se toca el blob: esto no es contenido, es una forma más de llegar a la clave que lo abre. ADR-022 reconciliar al importar, que es el que decide la Iteración 17, y de ahí lo que hay que tener presente sin abrirlo: la identidad de una entrada es HOST MÁS USUARIO y no nombre más usuario, medido sobre los ficheros reales —el criterio anterior veía 131 de los 261 grupos que hay—; el dominio registrable quedó descartado CON MEDIDA, porque funde dev, pre y producción del mismo sitio, que son credenciales distintas a propósito; la heurística AGRUPA Y PROPONE y decide quien importa, que es lo que la hace compatible con ADR-011; y la contraseña que no gana va al history de ADR-018 marcada con su origen, que es lo que impide que una candidata sin confirmar se presente como retirada. ADR-023 la extensión de navegador, que es el que decide la Iteración 18, y de ahí lo que hay que tener presente sin abrirlo: SOLO CHROME y SOLO CON EL PASSKEY, el mismo que da de alta la web, porque el 665 midió con Windows Hello real que desde la extensión da el mismo PRF y abre el mismo envoltorio; la contraseña maestra NO entra en la extensión y se sigue tecleando solo en la web. La clave vive NO EXTRAÍBLE en la memoria de un documento offscreen, que sobrevive a la muerte del service worker, y llega al popup por BroadcastChannel y nunca por chrome.runtime.sendMessage, que serializa a JSON; storage.session se descartó porque solo guarda JSON y obligaría a sacarla en bruto, y re-derivar en cada uso porque el desbloqueo con passkey admite cinco por hora y cuenta. El token vive con la clave y se revoca al bloquear. Es de SOLO LECTURA, rellena SOLO CON UN GESTO, en el marco principal y en el host de la entrada, y no declara content scripts. La instancia se fija al construirla, y la API no cambia: no hace falta CORS.

SEIS ADVERTENCIAS SOBRE EL ÍNDICE, agrupadas aquí a propósito para que no crezca una por cada renombrado: hay ADR que dicen cosas que después se corrigieron, y no se tocan porque son inmutables. ADR-012 sección 4 conserva dos lineamientos que ADR-016 dejó sin efecto. ADR-007 y ADR-019 citan claves del navegador que el 476 pasó a inglés; el rastro vivo está en CLAUDE.md y en web/src/lib/retiredStorage.ts. ADR-019 secciones 3 y 4 dicen que el caché es opt-in POR DISPOSITIVO, y la unidad no es el aparato sino el CONTENEDOR DE ALMACENAMIENTO: en iOS, Safari, Chrome y cada aplicación añadida a la pantalla de inicio guardan por separado, así que una copia activada en uno no existe en los otros DEL MISMO TELÉFONO; la decisión no cambia —sigue siendo opt-in y apagado por defecto— y la pantalla ya lo dice bien (546). Y ADR-023 sección 2.8 afirma que crypto.subtle aparece en un solo fichero, que era falso al escribirlo porque totp.ts lo usa desde la Iteración 13, y su sección 4 da por imposible verificar el ciclo entero del passkey con un autenticador virtual, que el 671 y el 674 desmintieron: lo que pierde el PRF es copiar la credencial entre pestañas, no navegar en la misma. Lo que esos apartados DECIDEN sigue en pie; lo que estaba mal eran los hechos que citaban.



Y LA LECCIÓN QUE DEJAN LAS DOS ÚLTIMAS, que es la que vale más que los avisos: una afirmación se repite de documento en documento sin comprobarla, y un documento aprobado y diferido es invisible por partida doble —no está en el código y tampoco en lo que se lee al empezar—. Pasó con ADR-018, que nombra su campo del blob HISTORIAL veinticuatro veces mientras el 542 retiraba esa regla siete días después de escribirlo, y con la Iteración 17, planificada proponiendo un campo nuevo para el historial que ADR-018 ya había decidido un mes antes. Ninguna de las dos llegó a morder, y las dos se descubrieron escribiendo otro documento.



Y LA SEXTA, encontrada al cerrar la 18: ADR-018 sección 2.5 y ADR-023 dicen que la caducidad del token en el servidor «sigue diferida», y está en vigor desde el 177, del 7 de agosto: cada token de sesión nace con 12 horas de vida, Sanctum rechaza el caducado —tiene test en SessionTest.php— y cada inicio de sesión barre los caducados de la cuenta. ADR-018 leyó 'expiration' => null en config/sanctum.php y no vio que la caducidad va en cada token. Lo que sí sigue diferido de esa sección es el endpoint para cerrar las demás sesiones. Salió porque los tokens de kastor bajaron de 5 a 2 durante el 675 sin que la extensión borrara ninguno.

Y una pregunta de la Iteración 14 que se anota sin tocar su archivo: su criterio 1 —abrir la vault en el móvil con kastor apagado— se dio por cumplido «desde un iPhone», y no consta si fue en la aplicación instalada o en el navegador. Si fue en el navegador, lo demostrado es algo distinto de lo que el criterio decía. Se resuelve mirando, y el sitio donde mirar es el 568.

Del 012 conviene tener presente una cosa sin abrirlo, porque decide si un despliegue funciona o no: HTTPS no es endurecimiento, es requisito de arranque. Fuera de localhost no existe crypto.subtle en contexto inseguro, así que una instancia servida por http en un dominio propio o en una IP de la red local no es una instalación limitada, es una donde no se puede ni registrar un usuario. Y la excepción de .localhost no rescata nada aquí: vale en la máquina que ejecuta el navegador, no desde otro dispositivo de la red.

Lo único de todo esto que hay que tener en la cabeza sin abrir nada, porque explica la forma de casi todo el código: la contraseña maestra no cifra los items. PBKDF2 con 600.000 iteraciones deriva del par contraseña y correo una clave maestra cuyo único trabajo es ENVOLVER una clave de vault aleatoria de 256 bits, y es esa la que cifra con AES-256-GCM. Por eso cambiar la contraseña maestra es reenvolver 32 bytes en vez de recifrar la vault, y por eso la clave de recuperación puede ser un segundo envoltorio de la MISMA clave sin duplicar nada. El hash que viaja al servidor se deriva de la clave maestra usando la contraseña como salt: quien lo capture consigue una sesión, no el contenido.

Y la consecuencia que más se malinterpreta, con test que falla si el aviso desaparece: rotar la contraseña maestra NO invalida la clave de recuperación, porque la clave de vault no cambia. Quien sospeche un robo tiene que regenerarla aparte.


DÓNDE ESTAMOS

LA ITERACIÓN 18 ESTÁ CERRADA desde el 16 de septiembre de 2026 y LA 19 ESTÁ SIN PLANIFICAR. Sus candidatos están en las secciones manuales de STATUS.md: el 680, Firefox; el 694, el 695 y el 696, la deuda del cierre; el 624, reconciliar sin red; la papelera de ADR-018, sin issue; y lo que la extensión dejó fuera de alcance. El detalle de la 18 y sus lecciones, en docs/planning/archive/ITERACION_18.md; lo que sigue mandando de ella, más abajo.

KASTOR ESTÁ DESPLEGADA con abfd0eb, el master del cierre, y la huella de las 669 entradas salió idéntica antes y después. La copia fuera de la máquina de antes de desplegar es la 65. La extensión para kastor NO se construye desde el repositorio tal cual: los nombres de la instancia se pasan en EVAULT_EXTENSION_ORIGINS al construir y no se escriben en ningún fichero versionado (ADR-023 sección 2.5). La build que usa quien tiene la vault está fuera del repositorio, en su disco de Windows, y hay que reconstruirla y recargarla en chrome://extensions cuando cambie el código de la extensión: no se actualiza sola al desplegar.


LO QUE SIGUE MANDANDO DE LO YA CERRADO

Aquí solo está lo que decide el trabajo de hoy. El detalle de cada iteración —qué se hizo, qué costó y qué se aprendió— vive en docs/planning/archive, un fichero por iteración, y ahí es donde hay que mirar antes de tocar lo que esa iteración construyó. Este bloque recorría de la 4 a la 17 y se recortó en el 664; lo que se quitó no se perdió, está en esos archivos.

QUÉ DECIDIÓ CADA UNA, PARA SABER CUÁL ABRIR. La 4, export e import, rotación de la maestra y clave de recuperación. La 5, que el proyecto se levante y se despliegue en otra máquina. La 6, que el código quede entero en inglés con comandos que lo comprueben. La 7, las contraseñas de verdad dentro de kastor. La 8, restaurar una copia y leer los items descifrados. La 9, alcanzar la vault desde fuera de casa. La 10, la conversión de comentarios y la jubilación del andamiaje que la vigilaba. La 11, que la vault larga deje de ir lenta. La 12, orden, favoritos y etiquetas. La 13, el segundo factor y la auditoría. La 14, la PWA y leer sin red. La 15, tarjetas y notas. La 16, el passkey. La 17, la reconciliación al importar. La 18, la extensión de Chrome.

DE LA VAULT REAL. Dentro de kastor hay 669 entradas de verdad desde el 11 de septiembre de 2026, y de ahí sale casi todo lo que hay que tener cuidado: lo que se rompa ahí NO es reproducible y el servidor no puede repararlo, porque no puede leer nada. Antes de desplegar, contar y copiar como dice la sección 7 de DEPLOYMENT.md —donde están además el reloj no monótono de esa máquina y que docker compose up -d --build no aplica las migraciones sin --force-recreate—; después, RECARGAR LA PESTAÑA, porque una abierta desde antes sigue con el código viejo.

DEL IMPORT Y LA RECONCILIACIÓN (17). LA IDENTIDAD DE UNA ENTRADA ES HOST MÁS USUARIO, con dos bordes escritos: una entrada guardada a mano SIN DIRECCIÓN se identifica por su nombre y no se reconoce contra una importada que sí la trae, y una entrada SIN USUARIO se identifica solo por su host, así que todas las de un host caen en un grupo —en la importación real juntó dos cuentas de Google distintas, que la pantalla ofreció como conflicto—. Los dos errores quedan del lado que la persona ve. REIMPORTAR ES UN GESTO SEGURO, y se importa por tandas: lo que ya está en la vault no se vuelve a escribir, y una fuente que no trae nada nuevo ofrece «Importar 0». Antes de importar algo grande, la predicción que vale es la del código real —parseImportFile, groupDuplicates y planImport— y no una aproximación.

EL HISTORIAL YA SE PUEDE OLVIDAR DE TODA LA VAULT (646), en «Contraseñas anteriores» del menú de usuario, que es la otra mitad de ADR-018 sección 2.2 —la de una entrada es del 621—. Dice cuántas contraseñas y en cuántas entradas antes de preguntar, avisa APARTE de las entradas con una candidata sin confirmar —ahí olvidar decide un conflicto—, escribe solo las entradas que tienen historial, y si algo corta a medias dice cuántas hizo y que repetirlo es seguro. Sin red no se ofrece. Lo hace el cliente porque el servidor no sabe qué entradas tienen historial: no puede leer ninguna.

DE LA EXTENSIÓN (18). Se desbloquea SOLO CON EL PASSKEY, el mismo que da de alta la web, y la contraseña maestra no entra nunca en ella. La clave vive no extraíble en un documento offscreen, llega al popup por BroadcastChannel y nunca por chrome.runtime.sendMessage, y se va con el bloqueo, con quince minutos sin usarla, al bloquear el equipo o al cerrar el navegador. Es de solo lectura, no declara content scripts y rellena solo con un gesto, en el marco principal y en el host exacto de la entrada. Cuatro cosas medidas que mandan sobre lo que venga: CHROME SOLO ACEPTA EL rpId SI HAY UN host_permission PARA ESE HOST SIN PUERTO, así que el manifiesto los genera sin puerto, con test; el portapapeles solo se escribe y se borra de verdad con un execCommand('copy') cuyo evento pone el texto —navigator.clipboard falla en silencio desde el offscreen y desde un popup sin foco—, y sin el permiso clipboardWrite nada funciona; activeTab SOLO LO CONCEDE UN CLIC REAL en el icono, y chrome.action.openPopup() no; y el autenticador virtual SÍ conserva el passkey y su PRF al navegar en la misma pestaña de la web a la extensión, que es lo que permite a verify-extension cubrir el ciclo entero. Lo que queda sin cubrir está escrito en el código: al rellenar, un campo tapado por otro elemento o recortado con clip-path.

DEL FORMULARIO Y DEL UTILLAJE (18). El segundo factor SE PLIEGA en las entradas sin semilla y no se retiró (669); una entrada con semilla se abre igual que antes, y typeTotpSeed despliega el campo antes de escribir. Los verificadores PREGUNTAN A LA API CUÁNTAS ALTAS LE QUEDAN antes de arrancar Chromium y register() falla si una ejecución registra más de las declaradas (667); el ajuste de desarrollo, THROTTLE_REGISTER_ATTEMPTS=1000, está en SETUP.md. Y ARCHIFY SE QUEDÓ PARA UN SOLO DIAGRAMA, el del README (668): la cadena para regenerarlo tiene un paso manual en el navegador que nada comprueba, y por eso no vienen más; cómo se hace, en docs/architecture/diagrams/README.md.

EL CAMPO history DEL BLOB es una lista de {password, date, origin}, con tres como mucho y ausente cuando está vacío. origin distingue una contraseña RETIRADA (rotation) de una CANDIDATA que nadie ha confirmado (import); la marca «sin confirmar» se calcula a partir de él y solo la apaga un gesto de quien tiene la vault. No sale en el CSV en claro y sí en el .evault. Está en FOUNDATION.md sección 2. De ADR-018 rige el historial; su papelera y su caducidad de token siguen diferidas.

DEL PASSKEY (16). LA CONTRASEÑA MAESTRA SIGUE SIENDO EL CAMINO PRINCIPAL: el passkey es un atajo revocable, como la clave de recuperación, y ninguna pantalla lo ofrece por delante. EL SERVIDOR NO VERIFICA WEBAUTHN, y es lo primero que un revisor va a mirar: recibe un hash donde esperaría una firma. El argumento es que el PRF solo se produce tras la verificación de usuario, así que poseerlo ES la prueba, y descansa en una premisa escrita como trigger de ADR-021: que el token solo trae ciphertext. Si algún día una sesión llega a leer o deducir contenido, ese apartado hay que rehacerlo y no heredarlo. UN PASSKEY ESTÁ ATADO AL NOMBRE DE HOST por el que se registró y esta instancia responde a dos, así que uno dado de alta entrando por la tailnet NO se ofrece entrando por evault.local —y la maestra sí—; está en DEPLOYMENT.md sección 8.5. ROTAR LA MAESTRA NO LOS REVOCA Y CAMBIAR EL CORREO SÍ, con una diferencia que decide el texto de la pantalla: un passkey NO SE PUEDE REHACER desde el servidor, porque su secreto vive dentro de un autenticador. Y EL PASSKEY CRUZA UNA FRONTERA QUE EL CACHÉ NO CRUZA: en el mismo iPhone, Chrome y la aplicación instalada NO comparten el caché offline y SÍ comparten el passkey, porque la credencial vive en el llavero del sistema y el envoltorio en el servidor.

SIN RED (14). LA DISTINCIÓN QUE SOSTIENE TODO LO OFFLINE, y si se rompe se rompe ADR-019 entero: cerrar sesión borra la copia de este dispositivo, BLOQUEAR NO. Recargar es un bloqueo, y después la vault tiene que seguir leyéndose sin red. El caché se indexa POR CORREO y no por id de usuario, porque al recargar no hay sesión y lo único que el navegador recuerda es el correo; está APAGADO POR DEFECTO, y el interruptor es una PANTALLA en /offline y no un conmutador; y SOLO SE CAE AL CACHÉ CUANDO NO LLEGÓ RESPUESTA —ApiError.isNetwork—: un 401 o un 429 SÍ llegaron al servidor. SIN RED NO SE ESCRIBE, y el rechazo vive en vault/api.ts y no en las pantallas, que es el único sitio por el que pasan las tres escrituras; pero EXPORTAR SIGUE FUNCIONANDO SIN RED y tiene test que falla si se rompe, porque poder sacar tus contraseñas vale MÁS cuando el servidor no contesta. NADA DE ESTO USA navigator.onLine: dice si el dispositivo está enganchado a una red, no si kastor responde. Lo que se usa es preguntar, GET /api/health.

DEL SEGUNDO FACTOR Y LA AUDITORÍA (13). La semilla vive en el campo totp del blob, tal como se pegó, y nunca sale en el export en claro, que además dice a cuántas entradas afecta. El contador de segundos NO cuenta como actividad del bloqueo, medido con reloj real en el caso 9 de verify-auto-lock. La auditoría NO puntúa la fuerza de una contraseña —puntuarla exige un diccionario, y una entropía ingenua daría nota alta justo a las malas—, así que informa de tres cosas que se ven sin adivinar, y su umbral de «corta» está en 12 y MEDIDO contra las contraseñas reales.

EL MAPA DEL CLIENTE, para no tener que buscarlo. La primitiva criptográfica es lib/vault/crypto.ts, el único sitio donde se deriva o se envuelve una clave que abre la vault; NO es el único que llama a crypto.subtle, porque totp.ts lo usa para el HMAC de los códigos, y cryptoSurface.test.ts falla si aparece un tercero. Encima está lib/vault/payload.ts, que cifra y descifra el contenido de los items. La clave vive en lib/vault/keyInMemory.ts, un store sin persist, y abrirla es unlockVault, en lib/vault/unlock.ts.

LO QUE LA SUITE NO PUEDE VER, para no confiar de más: jsdom no aplica CSS ni hace layout, así que la virtualización y cualquier cosa que dependa de CSS no se verifican ahí. Los tests de esas cosas comprueban la declaración, no el comportamiento, y lo llevan escrito encima. Lo que verifica de verdad es el navegador, y para eso están los cuatro verificadores de CLAUDE.md.

CINCO LECCIONES DE MÉTODO QUE SE PAGARON CARAS Y SIGUEN APLICANDO. EL CAMINO QUE NADIE RECORRE ES EL QUE ESTÁ ROTO, que salió cinco veces seguidas en la 5 y no se ve leyendo el código. CUANDO DOS MEDIDAS DISCREPAN, LA PRIMERA HIPÓTESIS NO PUEDE SER QUE LA RARA ES LA PROPIA: había un byte NUL que grep omitía en silencio, y se estuvo a punto de corregir un inventario correcto para ajustarlo a una herramienta rota. UN COMPROBADOR QUE OMITE FICHEROS EN SILENCIO ES PEOR QUE NO TENER COMPROBADOR, porque devuelve un cero tranquilizador; cualquier auditoría con grep tiene que usar -a. UN TEST VERDE EN LOS DOS SENTIDOS ES PEOR QUE NO TENER TEST, y la mutación hay que comprobar que se aplicó: una que no se aplica produce exactamente la misma tranquilidad falsa que el bug que busca. Y EXTRAPOLAR UNA MEDIDA NO ES MEDIRLA: el coste de la pantalla de revisión se midió a 120 entradas, dio ×2,5 y se dio por bueno; a 370 es ×7,2, porque las filas crecen con LO QUE ESTÁ MAL en la vault y no con la vault.


DEUDA CONOCIDA

Deuda sin issue no existe, así que aquí solo hay punteros. La lista viva es la de GitHub filtrando por el label deuda; esto es el resumen para no tener que ir a buscarlo.

LA DEUDA SE CIERRA DENTRO DE LA ITERACIÓN QUE LA ABRE, y así viene siendo desde la 12 con dos excepciones escritas: el 531, que la 15 dejó y la 17 cerró, y los tres que deja la 18. Al cerrar la 18: TRES issues de deuda abiertos. El 694: verify-extension no comprueba rellenar, que se verificó en navegador pero no se puede repetir con un comando. El 695: verify-auto-lock mira el aviso de los casos 7, 8 y 9 a los 14:45, con quince segundos de margen sobre el bloqueo, y salió dos veces 2 de 8 con los textos correctos porque la máquina se paró un minuto; hasta que se cierre, un rojo ahí se lee con la captura antes de creerlo. Y el 696: React 19.3 imprime «Encountered a script tag» en cada carga por el script de next-themes, sin efecto visible. y CERO alertas de Dependabot ABIERTAS. El 624 y el 680 no son deuda sino alcance que no cupo, con el motivo en SIGUIENTE PASO. El detalle de cada cierre vive en los archivos de iteración.

Este bloque tenía tres párrafos diciendo lo mismo con las fechas de la 10, la 12 y la 14, y se condensaron al cerrar la 16 por la regla que este documento se aplica a sí mismo: lo que no cabe aquí vive en otro sitio y se enlaza. Lo único que sobrevive de aquello porque sigue mandando es un comando, check-comment-language.py --all, en verde sobre el árbol entero y ejecutado por el CI en cada PR — lo que quedó en el sitio de la deuda más citada que este documento ha tenido.

Y el hosting compartido, que no tiene issue porque no es deuda sino una decisión pospuesta con criterio. Está descartado COMO VÍA DE ACCESO en ADR-015 y eso no se reabre; lo pospuesto es su uso como emplazamiento, con el disparador de ADR-013 sección 6 y tres señales que decidirán: cuántas veces no se pudo consultar la vault por estar kastor apagado, cuántas se recurrió al gestor anterior, y si Tailscale se desconecta solo.

No es deuda, aunque lo parezca: que el rate limiting cuente peticiones y no solo intentos fallidos. Se evaluó, se descartó con motivo y no hay intención de cambiarlo; está documentado en el código y en un test.


SIGUIENTE PASO

LO QUE PASA A LA 19, Y CON MOTIVO. El 624, RECONCILIAR SIN RED: necesita leer la vault entera del caché del dispositivo y abre dos preguntas —si el caché las tiene todas y qué hace una fusión que actualiza una entrada guardada sin conexión—; hoy importar sin red se rechaza como cualquier escritura, así que no hay un camino roto sino uno que no existe. Y LA PAPELERA DE ADR-018, que sigue diferida y se iba a decidir con la extensión delante: la extensión ya existe. De la otra mitad de ADR-018, la sesión, lo único que queda por hacer es el endpoint para cerrar las demás sesiones sin rotar la maestra; la caducidad de 12 horas NO está diferida, aunque ese ADR y ADR-023 lo digan —ver la sexta advertencia—. Y EL 680, FIREFOX, y EL 694, EL 695 y EL 696, la deuda del cierre de la 18.

Y EL CANDIDATO QUE SIGUE SIN CASO DE USO: las VAULTS COMPARTIDAS. ADR-008 las anticipa desde la Iteración 3 —la clave envuelta vive en vault_members y no en vaults justamente para eso— y lo único que falta es criptografía asimétrica. Hasta el 9 de septiembre el argumento era que hay dos cuentas reales en la instancia; el reset dejó una sola, así que espera a que la segunda vuelva.

LO QUE YA NO ESTÁ PENDIENTE, Y NO POR HABERSE HECHO: el criterio 2 de la Iteración 13 —un código de eVault aceptado por un servicio real— no se va a verificar, porque el 545 preguntó y nadie va a usar el segundo factor. Y el recuento de la auditoría, 246 de 369, se fue con la vault que medía. SU SUSTITUTO, medido en el 628: 512 DE 660 contraseñas tienen algo que corregir, 424 repetidas con UNA COMPARTIDA POR 70 ENTRADAS, 267 cortas y 77 de un solo tipo. Es el número sobre el que decidir —más 24 entradas sin confirmar—, y resolverlo es probar cada contraseña en su servicio y cambiar las repetidas: trabajo de quien tiene la vault, no de una iteración.

LO QUE NO HAY QUE REABRIR POR INERCIA: paginar GET /items, descartado con la medida delante en la 11; el acceso desde fuera de la red local, resuelto; el hosting compartido como vía de acceso, descartado en ADR-015; el panel Filament, fuera de alcance por ADR-009 sección 4; las carpetas, descartadas CON MEDIDA en la 13; el offline con escritura y cola de sincronización, descartado en ADR-019; los documentos adjuntos dentro del blob, descartados en ADR-020 sección 7; retirar el segundo factor, que el 545 decidió NO hacer con el dato delante y que el 669 pliega en vez de quitar; el dominio registrable como criterio de identidad, descartado CON MEDIDA en ADR-022 porque funde entornos dev, pre y producción; y Passwords de iOS como fuente, que no se descartó por diseño sino porque el fichero no existe.

Y LO QUE SE MIRA SIN QUE SEA UNA TAREA: las tres señales del hosting compartido como emplazamiento, con el disparador de ADR-013 sección 6, teniendo en cuenta que la Iteración 14 desactivó en parte la primera.


CONVENCIONES DE TRABAJO

Git: una rama por issue con el formato tipo/número-descripcion-corta. Merge a master solo mediante PR con squash, un commit por issue. El cuerpo del PR incluye Closes seguido del número para que GitHub cierre el issue automáticamente. Se usa gh CLI.

Definition of Done: criterios de aceptación completos, tests en verde, RBAC validado donde aplique, PR mergeado y este documento actualizado. STATUS.md no hay que tocarlo, lo regenera el CI tras el merge. Los issues con UI se verifican en navegador antes de marcarse como hechos.

Patrones de código heredados de un proyecto anterior: servicios de aplicación con método handle que reciben identificadores explícitos y no acceden a sesión. Double guard, es decir validación en la capa de presentación y también en la capa de aplicación, nunca solo en una. DTOs tipados para transferir datos entre capas. Servicios idempotentes para operaciones de agregación. Tests de aislamiento cross-tenant en todos los servicios críticos.
